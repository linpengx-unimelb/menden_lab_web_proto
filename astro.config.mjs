// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	site: 'https://linpengx-unimelb.github.io',
	base: '/menden_lab_web_proto',
});
